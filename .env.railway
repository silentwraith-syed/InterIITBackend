# Railway Environment Variables Template
# Copy these to Railway's environment variables section

# Database URL will be automatically provided by Railway PostgreSQL plugin
# DATABASE_URL=${{Postgres.DATABASE_URL}}

# JWT Secret (generate a strong random string)
JWT_SECRET=your-super-secret-jwt-key-change-in-production

# Allowed email domains
ALLOWED_DOMAINS=kgpian.iitkgp.ac.in,interiit.org

# Port (Railway will set this automatically, but fallback to 4000)
PORT=4000

# Node Environment
NODE_ENV=production
