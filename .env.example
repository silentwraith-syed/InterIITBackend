DATABASE_URL="postgresql://user:password@localhost:5432/kgptalks"
JWT_SECRET="your-super-secret-jwt-key"
ALLOWED_DOMAINS="kgpian.iitkgp.ac.in,interiit.org"
PORT=4000

# SMTP Configuration
# For Render/Production: Use SendGrid (Gmail SMTP is blocked on Render)
# Sign up: https://signup.sendgrid.com/
# Generate API Key: Settings → API Keys → Create API Key
# Verify Sender: Settings → Sender Authentication → Verify Single Sender

# SendGrid Configuration (Recommended for Production)
SMTP_HOST=smtp.sendgrid.net
SMTP_PORT=587
SMTP_USER=apikey
SMTP_PASS=SG.your-sendgrid-api-key-here
SMTP_FROM=syed.ahmed@gyws.org

# Gmail Configuration (Only works locally, NOT on Render)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-16-digit-app-password
# SMTP_FROM=your-email@gmail.com